[package]
name = "rustfly-inmemory"
version = "0.1.0-alpha.4"
edition = "2024"

[dependencies]
async-trait = "0.1.89"
bytes = "1.11.1"
rustfly-core = { path = "../rustfly-core", version = "0.1.0-alpha.4" }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
